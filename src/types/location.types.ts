export interface LocationInfo {
    latitude: number,
    longitude: number,
    name: string,
}

export interface LocationSearchInfo {
    latitude: number,
    longitude: number,
    name: string,
    fullName: string,
}